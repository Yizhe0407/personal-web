import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['imgur.com', 'img.icons8.com', 'cdn-icons-png.flaticon.com', 'cdn-icons-png.flaticon.com'], 
  },
};

export default nextConfig;
